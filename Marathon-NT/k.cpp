#include<bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    for(int tc=1;tc<=t;tc++)
    {
        // printf("Case %d: ",tc);
    
        int n,m;
        cin>>n>>m;
        vector<int> predator(m);
        for(int i=0;i<m;i++) cin>>predator[i];
        
    }
    return 0;
}